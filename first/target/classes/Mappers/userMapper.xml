<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.home.first.mapper.userMapper">
	<insert id="createUser" parameterType="com.home.first.dto.UserDto">
	INSERT INTO user(
		id,
		pw,
		email,
		hint
	)VALUES(
		#{id}, 
		#{pw}, 
		#{email}, 
		#{hint}
	)
	</insert>
	<select id="readUser" resultType="com.home.first.dto.UserDto">
	SELECT
		no,
		id,
		pw,
		email,
		hint,
		signdate,
		level
	FROM
		user
	WHERE
		id = #{id}
	</select>
	<update id="updateUser">
	UPDATE
		user
	SET
		pw = #{pw}
		email = #{email}
		hint = #{hint}
	WHERE
		id = #{id}	
	</update>
	<delete id="deleteUser" parameterType="com.home.first.dto.UserDto">
	DELETE FROM 
		user
	WHERE 
		id = #{id}
		pw = #{pw}
	</delete>
	<select id="listAllUser" parameterType="int" resultType="com.home.first.dto.UserDto">
	<![CDATA[
		SELECT
			*
		FROM
			user
		WHERE
			level = #{level}
	]]>
	</select>
</mapper>